#!/bin/bash
# True Slash Command: /chew
# Usage: ./_slash_chew [prompt]

PROJECT_ROOT="$(dirname "${BASH_SOURCE[0]}")"

if [ "$#" -eq 0 ]; then
    node "$PROJECT_ROOT/slash-command-parser.js" "/chew"
else
    node "$PROJECT_ROOT/slash-command-parser.js" "/chew $*"
fi
