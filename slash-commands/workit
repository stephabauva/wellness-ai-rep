#!/bin/bash
# Claude Code Slash Command: /workit
# Usage: ./workit [prompt]

# Change to parent directory to find slash-command-parser.js
cd "$(dirname "$0")/.."

if [ "$#" -eq 0 ]; then
    node slash-command-parser.js "/workit"
else
    node slash-command-parser.js "/workit $*"
fi
