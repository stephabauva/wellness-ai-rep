#!/bin/bash
# Claude Code Slash Command: /zapper
# Usage: ./zapper [prompt]

# Change to parent directory to find slash-command-parser.js
cd "$(dirname "$0")/.."

if [ "$#" -eq 0 ]; then
    node slash-command-parser.js "/zapper"
else
    node slash-command-parser.js "/zapper $*"
fi
