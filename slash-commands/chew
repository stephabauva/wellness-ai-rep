#!/bin/bash
# Claude Code Slash Command: /chew
# Usage: ./chew [prompt]

# Change to parent directory to find slash-command-parser.js
cd "$(dirname "$0")/.."

if [ "$#" -eq 0 ]; then
    node slash-command-parser.js "/chew"
else
    node slash-command-parser.js "/chew $*"
fi
