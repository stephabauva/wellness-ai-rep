#!/bin/bash
# Claude Code Slash Command: /mobile-ux
# Usage: ./mobile-ux [prompt]

# Change to parent directory to find slash-command-parser.js
cd "$(dirname "$0")/.."

if [ "$#" -eq 0 ]; then
    node slash-command-parser.js "/mobile-ux"
else
    node slash-command-parser.js "/mobile-ux $*"
fi
