#!/bin/bash
# True Slash Command: /mobile-ux
# Usage: ./_slash_mobile-ux [prompt]

PROJECT_ROOT="$(dirname "${BASH_SOURCE[0]}")"

if [ "$#" -eq 0 ]; then
    node "$PROJECT_ROOT/slash-command-parser.js" "/mobile-ux"
else
    node "$PROJECT_ROOT/slash-command-parser.js" "/mobile-ux $*"
fi
