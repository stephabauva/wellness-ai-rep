#!/bin/bash
# True Slash Command: /arch-guard
# Usage: ./_slash_arch-guard [prompt]

PROJECT_ROOT="$(dirname "${BASH_SOURCE[0]}")"

if [ "$#" -eq 0 ]; then
    node "$PROJECT_ROOT/slash-command-parser.js" "/arch-guard"
else
    node "$PROJECT_ROOT/slash-command-parser.js" "/arch-guard $*"
fi
